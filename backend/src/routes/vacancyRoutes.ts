import { Router } from 'express';

const router = Router();

// TODO: Implement vacancy routes
router.get('/', (req, res) => {
  res.json({ message: 'Vacancy routes - to be implemented' });
});

export default router; 